package database.db_sixth_topic.design_patterns.decorator_pattern;

/**
 * Decorator Pattern:
 * Java decorator design pattern comes under structural design patterns, and it provides the facility
 * to add additional responsibilities to an existing object without altering its structure.
 * It uses composition to extend the facility to an existing objects.
 */
public interface InformationDisplay {
    public void display(Integer id);
}
